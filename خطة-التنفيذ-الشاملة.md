# خطة التنفيذ الشاملة — منصة قِرطاس
### موقع + تطبيقات Flutter + باك-اند موحّد + خط جذب كتب آلي + نشر مجاني
**الإصدار 1.0 — للمناقشة والموافقة قبل التنفيذ**

---

## 1) المعمارية العامة: باك-اند واحد يخدم كل العملاء

```
                    ┌────────────────────────────┐
                    │      الباك-اند الموحد       │
                    │  Firebase (مشروع واحد مجاني) │
                    │  • Auth — تسجيل الدخول       │
                    │  • Firestore — ميتاداتا +     │
                    │    مواضع القراءة + المفضلة    │
                    │  • Analytics — القياس         │
                    │  • Hosting — صفحات SEO        │
                    └───────────┬────────────────┘
                                │ نفس المشروع، نفس البيانات
        ┌───────────────────────┼───────────────────────┐
        │                       │                       │
┌───────┴────────┐    ┌─────────┴────────┐    ┌─────────┴────────┐
│   الموقع (ويب)  │    │ تطبيق Flutter     │    │  PWA (الحالي)    │
│  SEO + قارئ     │    │ Android/iOS/ويندوز│    │  يعمل الآن فوراً  │
│  سريع خفيف      │    │ ماك/لينكس/ويب     │    │  نسخة احتياطية   │
└───────┬────────┘    └─────────┬────────┘    └──────────────────┘
        │                       │
        └───────────┬───────────┘
                    ▼
    ┌──────────────────────────────────────┐
    │  ملفات الكتب: ثابتة على CDN مجاني     │
    │  (Cloudflare Pages — نقل بيانات بلا حد│
    │   مجاني) — قراءتها لا تستهلك حصة DB   │
    └──────────────────────────────────────┘
```

**المبادئ الحاكمة:**
1. **باك-اند واحد**: مشروع Firebase واحد — كل العملاء (ويب، أندرويد، iOS، سطح مكتب) يتصلون به بنفس الحساب ونفس بيانات القراءة. تفتح الكتاب على الهاتف وتكمل من المتصفح.
2. **الكتب ملفات ثابتة، ليست في قاعدة البيانات**: نصوص الكتب JSON مضغوطة على CDN مجاني — الحصة المجانية للـDB تبقى للمزامنة فقط (نفس المبدأ الذي ثبت نجاحه في MVP).
3. **صفر تكلفة حقيقية**: كل مكوّن له خطة مجانية معلنة، وأي تكلفة لاحقة (دومين 10$، Play بـ25$) تُدفع من أول إيراد إعلانات.

---

## 2) خط جذب الكتب من الإنترنت (Content Pipeline) — القلب التشغيلي

### 2.1 المصادر (قانونية 100% — ملكية عامة أو رخص حرة)

| المصدر | اللغة | الحجم | الجودة | الوضع القانوني |
|---|---|---|---|---|
| **Project Gutenberg** | إنجليزي | 70,000+ كتاب | جيد (نصوص قديمة بعضها بحاجة تنظيف) | ملكية عامة صريحة — الأفضل تقنياً: كتالوج RDF كامل قابل للسحب آلياً |
| **Standard Ebooks** | إنجليزي | ~1,000 كتاب | ممتازة (منسق يدوياً) | ملكية عامة (CC0 للتنسيق) — مثالي للكتب المميزة |
| **ويكي مصدر العربية** | عربي | آلاف الصفحات | جيدة جداً (نصوص مدققة مجتمعياً، بعضها مقرون بالمخطوط) | ملكية عامة — **المصدر الأول للعربي قانونياً**، وعنده API مجاني |
| **هنداوي** | عربي | ~4,000 كتاب | ممتازة | ⚠️ رخصتهم CC BY-NC-ND على **نسختهم الإلكترونية** (منع التعديل) — لا ننسخ ملفاتهم؛ النصوص الأصلية ملكية عامة فنسحب النص من ويكي مصدر/مصادر أصلية ونستخدم إصدار هنداوي كمرجع تدقيق فقط |
| **المكتبة الشاملة** | عربي | ضخم | متفاوتة | ⚠️ مختلط: كثير من كتبها ليس ملكية عامة — تدخل يدوي لكل كتاب (فحص سنة وفاة المؤلف) |
| **LibriVox** (مرحلة لاحقة) | صوتي | 20,000+ | جيد | ملكية عامة |

### 2.2 خط المعالجة الآلي (Node.js — مجاني بالكامل)

```
[1] Harvest     سحب الكتالوجات: Gutenberg RDF / ويكي مصدر API
     ↓
[2] Legal Filter  فلتر آلي: وفاة المؤلف + رخصة + استبعاد المحظور
     ↓
[3] Clean       استخراج النص، حذف الترويسات، توحيد علامات الحوار
                والاقتباس، معالجة RTL، تقسيم الفصول، ضغط JSON
     ↓
[4] Enrich      غلاف مولّد من الهوية، تصنيف، وصف عربي/إنجليزي،
                كلمات مفتاحية، فهرس تفاعلي
     ↓
[5] QA          فحص آلي (طول، نسبة الرموز الشاذة، اتجاه) + عينة
                بشرية 10% + زر «أبلغ عن خطأ» داخل القارئ يغذي الإصلاح
     ↓
[6] Publish     ملفات الكتب → CDN | ميتاداتا → Firestore | صفحات
                SEO → الموقع | تحديث PWA والتطبيق تلقائياً (كتالوج مشترك)
```

**التنفيذ:** حزمة `tools/pipeline/` في المشروع بأوامر:
```bash
npm run books:harvest -- --source=gutenberg --limit=100
npm run books:clean -- --id=1342        # Pride and Prejudice
npm run books:publish                   # رفع للـCDN + تحديث DB + الموقع
npm run books:report                    # تقرير الحالة والجودة
```

**الأتمتة المجانية:** GitHub Actions أسبوعياً يشغّل Harvest → يفتح Pull Request بكتب مرشحة جديدة → تراجعها في دقائق → دمج يفجر المحتوى للموقع والتطبيق معاً (حد GitHub Actions المجاني: 2,000 دقيقة/شهر — نحتاج ~20).

**أهداف المحتوى:**
- **أسبوع 1 (الإطلاق):** 100 كتاب (60+ إنجليزي من Gutenberg آلياً + 30+ عربي من ويكي مصدر)
- **نمو أسبوعي:** +25 كتاباً مؤتمتاً
- **3 أشهر:** 500+ كتاب مع قسم «مختارات منسقة» أسبوعي

---

## 3) الموقع (التنفيذ يبدأ به)

**مهمته المزدوجة:** بوابة القراءة الفورية + **محرك النمو المجاني (SEO)** — صفحة مستقلة لكل كتاب تلتقط ملايين عمليات البحث عن أسماء الكلاسيكيات.

### ⚠️ قرار تقني جوهري — مراجعة طلب «كل شيء بـFlutter»:
Flutter ممتاز للتطبيقات، لكن **Flutter web يضعف خطة النمو المجانية**:
- الصفحات تُبنى في المتصفح (client-side) → جوجل يفهرسها أضعف بكثير
- أول تحميل 2MB+ (إطار Flutter) → القارئ العربي على شبكات بطيئة يغادر
- سلوك التمرير والروابط غير طبيعي للمحتوى النصي الطويل

| الخيار | المزايا | العيوب | التقييم |
|---|---|---|---|
| **A. موقع مولّد ثابتاً (Astro/HTML مولّد بـNode) + Flutter للتطبيقات** | سرعة قصوى، SEO ممثالي، إعادة استخدام محرك قارئ MVP، نشر مجاني بلا حدود | لغتان تقنيتان (لكن منطق القارئ مشترك) | ⭐ **الموصى به** |
| B. Flutter web للموقع + صفحات SEO ثابتة منفصلة | كود Flutter موحد | تعقيد مزدوج + زوار SEO يدخلون لموقع مختلف عن تطبيق الويب | مقبول |
| C. كل شيء Flutter web | كود واحد حرفياً | SEO ضعيف يقتل قناة النمو الأولى | ❌ لا يوصى به |

**بنية الموقع (الخيار A):**
```
site/  ← مولّد بـNode بسيط (بلا إطار ثقيل — مكتبات = صفر)
├── /                الصفحة الرئيسية: مختارات + تصنيفات + بحث
├── /book/<id>/      صفحة الكتاب (SEO): وصف، فهرس، غلاف، زر «اقرأ الآن»
├── /read/<id>/<ch>/ القارئ نفسه — محرك MVP المطوَّر (أعمدة RTL/LTR)
├── /category/<c>/   صفحات التصنيفات (SEO)
├── /about, /sitemap.xml, /robots.txt, RSS
```
كل صفحة كتاب = HTML ثابت يُولّد من ميتاداتا Firestore/JSON — مئات الصفحات بأمر `npm run site:build` ونشر بثوانٍ.

---

## 4) تطبيقات Flutter

- **كود واحد** → Android (APK/AAB) + iOS + Windows + macOS + Linux
- الحزم: `google_mobile_ads` (AdMob)، `firebase_core/auth/cloud_firestore`، `shared_preferences` + `sqflite` (تخزين محلي)، محرك ترقيم مخصص (نفس خوارزمية أعمدة MVP — مثبتة عربياً)
- شاشات: المكتبة/البحث/القارئ/الإعدادات — نفس تصميم MVP الحالي (لقطاته معتمدة)
- **تسلسل الإطلاق المالي:** أندرويد APK مباشر + Amazon/Samsung/Huawei (0$) أولاً → Play بـ25$ من أول إيراد AdMob → iOS (99$/سنة) آخر ما يُدفع
- المراحل: أسبوعان إطار العمل والشاشات → أسبوع القارئ والمزامنة → أسبوع AdMob والنشر

---

## 5) خطة النشر المجانية — بالتفصيل

| المكوّن | الخدمة | الحد المجاني | يكفي حتى |
|---|---|---|---|
| الموقع + ملفات الكتب (CDN) | **Cloudflare Pages** | نقل بيانات **غير محدود** مجاناً، 500 بناء/شهر | ملايين الزيارات |
| صفحات SEO الإضافية | Firebase Hosting | 10GB تخزين، نقل يومي سخي | — |
| الباك-اند (Auth + DB) | **Firebase Spark** | 50k قراءة/يوم، 20k كتابة/يوم | ~5 آلاف مستخدم نشط يومياً |
| خط المحتوى الآلي | **GitHub Actions** | 2,000 دقيقة/شهر (نحتاج ~20) | للأبد تقريباً |
| مستودع الكود | GitHub | مستودعات غير محدودة | — |
| التحليلات | Google Analytics 4 | بلا مقابل | — |
| متاجر | APK مباشر + Amazon + Samsung + Huawei | 0$ | Play بـ25$ من الإيراد |
| الدومين | `<qirtas>.pages.dev` مجاناً | — | دومين مدفوع 10$/سنة اختيارياً |

**خطوات النشر (ستنفَّذ بهذا الترتيب بعد موافقتك):**
1. إنشاء مستودع GitHub + ربط المشروع
2. ربط Cloudflare Pages بالمستودع → الموقع حي على رابط مجاني خلال دقائق
3. إنشاء مشروع Firebase → تشغيل Auth + Firestore بقواعد أمان مقفلة
4. تشغيل Pipeline → أول 100 كتاب → إعادة بناء الموقع
5. AdMob: إنشاء الحساب والوحدات → استبدال الإعلانات التجريبية بمعرفات اختبار ثم حقيقية
6. بناء APK → توزيع مباشر + رفع Amazon/Samsung/Huawei
7. ربط الدومين المدفوع اختيارياً عند أول إيراد

> ⚠️ **ملاحظة عملية للنشر:** إنشاء الحسابات (GitHub / Cloudflare / Firebase / AdMob) يتطلب دخولك ببريدك — سأجهّز كل شيء محلياً بجاهزية «زر نشر»، ثم ننفذ الربط معاً: إما عبر متصفحك مباشرة (أسجل معك الخطوات وأكمّل آلياً بعد دخولك) أو أسلّمك أوامر جاهزة تنفذها بدقيقة لكل حساب.

---

## 6) الجدول الزمني (بعد الموافقة)

| الأسبوع | المخرجات |
|---|---|
| **1** | الموقع مكتمل ومنشور برابط حي + Pipeline يعمل + أول 100 كتاب + Firestore مربوط |
| **2–3** | تطبيق Flutter: الإطار والشاشات + القارئ + المزامنة + APK تجريبي |
| **4** | AdMob حقيقي + النشر بالمتاجر المجانية + إعلان الإطلاق |
| **5+** | محتوى +25 كتاباً أسبوعياً آلياً + تحسينات من بيانات GA4 |

---

## 7) المخاطر وضوابطها

| الخطر | الضابط |
|---|---|
| نصوص عربية آلية رديئة | البدء من ويكي مصدر المدقق فقط + فحص عينة إلزامي + زر إبلاغ القراء |
| رخصة هنداوي NC-ND | عدم نسخ ملفاتها — مرجع تدقيق فقط، والمصدر الأصلي ملكية عامة |
| تجاوز حصة Firebase | مبدأ «الكتب ثابتة» + تنبيه 70% + ترقية من الإيراد |
| Flutter web إن اختير | صفحات SEO منفصلة إلزامياً (الخيار B) |
| تعثر حسابات النشر | تجهيز «زر نشر» محلياً كاملاً حتى لا يتوقف شيء على التسجيل |

---

## القرارات المطلوبة قبل التنفيذ (تُناقش في المحادثة)
1. تقنية الموقع: A / B / C (الأسبقية: A)
2. الباك-اند: Firebase أم Supabase (الأسبقية: Firebase لتكامله الفوري مع Flutter وAdMob)
3. أولوية المحتوى العربي: ويكي مصدر أولاً (آمن أبطأ) أم مزيج أسرع بفحص يدوي
4. طريقة تنفيذ النشر: معاً عبر متصفحك، أم تسليم حزم جاهزة بأوامر
